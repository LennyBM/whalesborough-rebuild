/**
 * Sanity schema registry.
 * Wave 2 will register schemas for: journal posts, FAQs, banner alerts,
 * homepage hero rotation, lodge-sales copy blocks, and seasonal content.
 */
export const schemaTypes = [] as const;
